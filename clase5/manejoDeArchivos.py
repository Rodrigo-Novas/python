import os
import shutil
import subprocess #ejecutas subprocesos
# print(os.listdir("c:")) #Imprime los archivos en carpeta
# if os.path.exist("c:/hecho") == false:
#     os.mkdir("c:/hecho") #Esto lo que hace es crear un directorio
# print(os.pathsep)

# shutil.copy("origen", "destino")
# shutil.mode("origen", "destino")
# shutil.rmtree("path")#elimina carpeta no vacia, elimina todo cuidado


#Ejecutar procesos (googlear)
# subprocess.run(["mkdir", "argumentos"], shell = true)

# p= subprocess.run("hostname", capture_output=True, encoding ="cp850")